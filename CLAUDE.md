# Puffins website — working agreement

- This worktree lives at `D:\Projects\Puffins\main` (moved 2026-09-19 from
  `D:\THE_DATA\Parth bana`; the sibling `gh-pages` worktree is at `D:\Projects\Puffins\live`,
  moved from `D:\THE_DATA\puffins-gh-pages`). Unrelated personal files that used to sit
  alongside this repo (images, PDFs, scripts, an installer) were moved back to
  `D:\THE_DATA\Parth bana` during that move — this directory now holds only the Puffins repo.
- After making ANY change to files under `site/` (including small tweaks), commit and push to
  the `origin` remote (GitHub: `hemantpremgaatha/puffins`) on the `main` branch — do this
  automatically, without asking for confirmation first. The user has given standing
  authorization for this.
- Only stage/commit files actually related to the change (the `site/` files touched).
- Keep local `main` in sync with `origin/main` at all times — push right after each commit.
