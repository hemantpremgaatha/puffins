# Puffins website (gh-pages) — working agreement

- After making ANY change to files in this worktree (including small tweaks), commit and push
  to the `origin` remote (GitHub: `hemantpremgaatha/puffins`) on the `gh-pages` branch —
  automatically, without asking for confirmation first. The user has given standing
  authorization for this (same agreement as the `main` worktree at `D:\THE_DATA\Parth bana`).
- This worktree holds the built/deployed site directly at the repo root (no `site/`
  subfolder) — stage only the files actually changed.
- Keep local `gh-pages` in sync with `origin/gh-pages` at all times — push right after each
  commit.
